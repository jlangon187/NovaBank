spring:
  config:
    import: ""
  cloud:
    config:
      enabled: false
  r2dbc:
    url: r2dbc:h2:mem:///cliente_test_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
eureka:
  client:
    enabled: false
