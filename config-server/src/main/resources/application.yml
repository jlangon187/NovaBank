server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/Javil/Desktop/NovaBank/novabank-config-repo
          default-label: config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/